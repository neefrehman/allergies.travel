/* eslint-disable import/no-extraneous-dependencies */
import "@testing-library/cypress/add-commands";

// ***********************************************
// This example commands.js shows you how to
// create and overwrite commands.
//
// For more comprehensive examples read more here:
// https://on.cypress.io/custom-commands
// ***********************************************

// -- This is a parent command --
// Cypress.Commands.add("login", (email, password) => { ... })
//
//
// -- This is a child command --
// Cypress.Commands.add("drag", { prevSubject: 'element'}, (subject, options) => { ... })
//
//
// -- This is a dual command --
// Cypress.Commands.add("dismiss", { prevSubject: 'optional'}, (subject, options) => { ... })
//
//
// -- This will overwrite an existing command --
// Cypress.Commands.overwrite("visit", (originalFn, url, options) => { ... })

// import mockTranslations from "shared/mock-data/translations.json";
// import { getLabel } from "shared/labels";

// Cypress.Commands.add("getLabel", (key) =>
//   getLabel(mockTranslations.dictionary, key)
// );
